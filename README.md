# evening_report_bot

Telegram long-polling бот для вечірніх звітів магазину (Java 17 + Maven, без Spring Boot).

## Можливості
- Реєстрація співробітника (`/start`) з кроками ПІБ, місто, адреса, бренд.
- Перегляд і редагування профілю (`/profile`, `/profile_edit`).
- Майстер створення звіту (`/report` або `/report YYYY-MM-DD`).
- Продовження чернетки після перезапуску (`/draft`).
- Перегляд останнього фінального звіту (`/last`).
- Кнопки меню:
  - `🧾 Створити звіт`
  - `📌 Продовжити чернетку`
  - `📄 Останній звіт`
  - `👤 Профіль`
- Локальне збереження у JSON (каталог `./data`):
  - `users.json`
  - `drafts.json`
  - `reports.json`
- Атомарний запис файлів (temp + rename).

## Змінні середовища
```bash
export BOT_TOKEN=...
export BOT_USERNAME=...
```

## Запуск
```bash
mvn clean package
java -jar target/evening_report_bot-1.0.0.jar
```

## Команди
- `/start` — старт і реєстрація
- `/profile` — перегляд профілю
- `/profile_edit` — редагувати профіль
- `/report` — новий звіт (дата за замовчуванням Europe/Kyiv)
- `/report YYYY-MM-DD` — новий звіт на вказану дату
- `/draft` — продовжити чернетку
- `/last` — останній фінальний звіт
- `/back` — крок назад у майстрі
- `/cancel` — скасувати поточну дію

## Архітектура
- `domain` — моделі: `EmployeeProfile`, `ReportDraft`, `FinalReport`, `ProductBreakdown`
- `storage` — JSON storage/repositories
- `service` — бізнес-логіка обчислень/форматування
- `ui` — стани майстра, повідомлення, клавіатури
- `bot` — Telegram bot bootstrap + router

## Тести
Запуск:
```bash
mvn test
```
Є базові unit-тести для:
- conversion %
- average check
- plan/fact %
